# saas-project-dashboard

> Real-time project metrics dashboard — React + Node.js/Express + WebSocket live updates.

## Stack
Node.js 20 · Express · React 18 · SQLite · WebSocket · JWT

## Quick Start

```bash
cd backend && npm install && npm run dev
# In another terminal:
cd frontend && npm install && npm start
```
